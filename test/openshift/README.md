# OpenShift testing

1. Install Dagger CLI (1.0.0-beta.3)
2. Run `dagger fn call check`

## Reviewing results

Run `dagger fn call check export --path ./report` to get a directory of container image and chart verification reports.

## Submitting results

- Containers: TODO
- Charts: TODO
